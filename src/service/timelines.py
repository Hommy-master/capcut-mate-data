from src.utils.logger import logger
from typing import List, Dict, Any, Tuple


def timelines(draft_url: str, audio_infos: str) -> Tuple[str, str, List[str]]:


    return draft_url, track_id, audio_ids