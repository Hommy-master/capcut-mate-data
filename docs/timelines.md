# TIMELINES API 接口文档

## 接口信息

```
POST /openapi/capcut-mate/v1/timelines
```

## 功能描述

根据指定的总时长和分割数量，将时间线按平均或随机方式分割成多个时间段。该接口主要用于视频编辑场景中的时间线规划，可以帮助用户合理分配视频内容的时间段。

## 更多文档

📖 更多详细文档和教程请访问：[https://docs.jcaigc.cn](https://docs.jcaigc.cn)

## 请求参数

```json
{
  "duration": 10000000,
  "num": 2,
  "start": 0,
  "type": 0
}
```

### 参数说明

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| duration | integer | ✅ | - | 总时长（微秒） |
| num | integer | ✅ | - | 时间线的个数，值为2即表示将总时长分为2个小段 |
| start | integer | ✅ | - | 开始时间（微秒） |
| type | integer | ✅ | - | 分割类型：0-平均分，1-随机分 |

### 参数详解

#### 时间参数

- **duration**: 总时长，单位为微秒（1秒 = 1,000,000微秒）
- **start**: 时间线的起始时间，单位为微秒
- **num**: 需要分割的时间段数量
- **type**: 分割方式
  - 0 = 平均分割：将总时长平均分成指定数量的段
  - 1 = 随机分割：随机将总时长分割成指定数量的段

## 响应格式

### 成功响应 (200)

```json
{
  "timelines": [
    {
      "end": 5000000,
      "start": 0
    },
    {
      "end": 10000000,
      "start": 5000000
    }
  ],
  "all_timelines": [
    {
      "end": 10000000,
      "start": 0
    }
  ]
}
```

### 响应字段说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| timelines | array | 分割后的时间线列表，每个元素包含start和end字段 |
| all_timelines | array | 完整的时间线列表，包含整个时间范围 |

### 错误响应 (4xx/5xx)

```json
{
  "detail": "错误信息描述"
}
```

## 使用示例

### cURL 示例

#### 1. 平均分割时间线

```bash
curl -X POST https://capcut-mate.jcaigc.cn/openapi/capcut-mate/v1/timelines \
  -H "Content-Type: application/json" \
  -d '{
    "duration": 10000000,
    "num": 2,
    "start": 0,
    "type": 0
  }'
```

#### 2. 随机分割时间线

```bash
curl -X POST https://capcut-mate.jcaigc.cn/openapi/capcut-mate/v1/timelines \
  -H "Content-Type: application/json" \
  -d '{
    "duration": 15000000,
    "num": 3,
    "start": 1000000,
    "type": 1
  }'
```

## 错误码说明

| 错误码 | 错误信息 | 说明 | 解决方案 |
|--------|----------|------|----------|
| 400 | duration是必填项 | 缺少duration参数 | 提供有效的总时长 |
| 400 | num是必填项 | 缺少num参数 | 提供有效的分割数量 |
| 400 | start是必填项 | 缺少start参数 | 提供有效的开始时间 |
| 400 | type是必填项 | 缺少type参数 | 提供有效的分割类型 |
| 400 | num必须大于0 | 分割数量无效 | 使用大于0的分割数量 |
| 500 | 服务器内部错误 | 内部处理错误 | 联系技术支持 |

## 注意事项

1. **时间单位**: 所有时间参数使用微秒（1秒 = 1,000,000微秒）
2. **分割数量**: num必须大于0
3. **随机分割**: 随机分割的结果每次可能不同
4. **平均分割**: 平均分割保证每段时长基本相等
5. **边界处理**: 最后一段会自动调整以确保总时长准确

## 工作流程

1. 验证必填参数（duration, num, start, type）
2. 根据分割类型选择处理方式
3. 计算时间线分割点
4. 构造响应数据
5. 返回处理结果

## 相关接口

- [创建草稿](./create_draft.md)
- [添加视频](./add_videos.md)
- [添加图片](./add_images.md)
- [保存草稿](./save_draft.md)
- [生成视频](./gen_video.md)

---

<div align="right">

📚 **项目资源**  
**GitHub**: [https://github.com/Hommy-master/capcut-mate](https://github.com/Hommy-master/capcut-mate)  
**Gitee**: [https://gitee.com/taohongmin-gitee/capcut-mate](https://gitee.com/taohongmin-gitee/capcut-mate)

</div>